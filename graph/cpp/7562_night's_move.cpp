#include<iostream>
#include<queue>
#include<utility>
using namespace std;

typedef pair<int> ii;
typedef vector<int> vi;

int dr[] = {2,}

int main() {
    int T, L;
    cin >> T;
    for(int i = 0; i < T; i++) {
        int x, y;
        ii cur, fin;
        cin >> L;
        int board = [L][L];
        
        cin >> x >> y;
        cur = make_pair(x, y);
        cin >> x >> y;
        fin = make_pair(x, y);
        
        
    }
}